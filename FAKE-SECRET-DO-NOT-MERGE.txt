THROWAWAY FILE — DELETE THIS BRANCH.

This is a NORTHSTAR RED-RUN probe. It exists to prove that the secret scan
wired in PR #9 can actually FAIL. A guard nobody has watched fail is not
evidence -- it may be measuring shape.

The string below is a SYNTHETIC, NEVER-VALID test credential. It matches the
SHAPE of a GitHub personal access token so gitleaks' `github-pat` rule fires,
but its body is the literal word FAKE repeated. It has never been issued, it
authenticates nothing, and it is not derived from any real key.

token = "ghp_FAKEfakeFAKEfakeFAKEfakeFAKEfake1234"

Expected: the `Secret scan / gitleaks` check goes RED on this PR.
